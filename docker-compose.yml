services:
  postgres:
    image: postgres:16
    ports:
      - "5555:5432"
    environment:
      POSTGRES_DB: nova_auth_test
      POSTGRES_PASSWORD: root
    tmpfs:
      - /var/lib/postgresql/data
